#include "{!name!}.h"

void {!name!}::component_thread(void){
  while(true){
    {!input_thread!}
    //Clock Lo
    c->clock_lo(reset);

    {!output_thread!}
    //Clock Hi
    c->clock_hi(reset);
    wait(Step);
  }
}

